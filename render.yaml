services:
  - type: web
    name: telegram-support-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
    # Автоматически определит порт 5000
